import React, { ReactNode } from 'react';

export const endMessage: ReactNode = (
  <p
    style={{
      textAlign: 'center',
      marginRight: '10px',
      marginTop: '10px',
      marginBottom: '8px',
    }}
  >
    <b> No more Incidents</b>
  </p>
);
